Generate developer documentation and user documentation for the feature called $ARGUMENTS

Analyze all the relevant files in the project. Exclude non-code and code from external sources such as third party libraries.
For the developer documentation generate a file in docs/dev/[named-of-the-feature].md
For the user documentation generate a file in docs/user/[named-of-the-feature].md
Follow project's existing documentation patterns.
Include placeholders for screenshots in user docs.
Add proper cross-references between the two doc types.
